<div class="bg-blue-100 w-full">
  <%= @field.fields_for @delivery_options do |option| %>
    <%= option.association :option, label: 'Délais de livraison', collection: Urgence.all, selected: Urgence.first, input_html: {'data-reflex': 'change->Delivery#urgence'} %>
  <% end %>
</div>
